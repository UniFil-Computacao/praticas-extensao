import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fast-and-flash',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
